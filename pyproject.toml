[project]
name = "vulndjangoapp"
version = "0.1.0"
description = "Django application with known vulnerabilities for security testing"
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "django==3.2.0",  # Has multiple critical CVEs including SQL injection, XSS, and security bypass
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[dependency-groups]
dev = []
